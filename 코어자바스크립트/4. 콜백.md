## 1️⃣ 콜백 함수란?

**콜백 함수**는

다른 함수의 **인자로 전달되어**,

그 함수 내부에서 나중에 호출되는 함수다.

즉, 실행을 직접 하는 것이 아니라

**제어권을 넘겨받은 코드에 의해 실행된다.**

---

## 2️⃣ 제어권

콜백의 핵심은 **제어권을 위임한다는 것**이다.

```tsx
setInterval(function () {
  console.log('실행');
}, 1000);
```

여기서:

- 콜백 함수 → `function () { console.log('실행'); }`
- 제어권을 가진 주체 → `setInterval`

---

### 📌 setInterval 구조

```tsx
setInterval(callback, delay);
```

### 제어권을 가진 코드가 결정하는 것

1. **언제 실행할지**
2. **어떤 인자를 넘길지**
3. **this를 무엇으로 할지**

---

## 3️⃣ 콜백 함수도 결국 함수다

콜백은 특별한 문법이 아니라

그냥 "함수"일 뿐이다.

```tsx
function greet(name) {
  console.log('Hello ' + name);
}

function execute(callback) {
  callback('World');
}

execute(greet);
// Hello World
```

✔ 일반 함수

✔ 익명 함수

✔ 화살표 함수

모두 콜백으로 사용 가능

---

## 4️⃣ 콜백 내부의 this

콜백도 함수이므로

기본적으로는 **전역 객체를 바라본다.**

하지만 제어권을 가진 함수가

`this`를 지정하면 그 값을 따른다.

---

### 🔹 예시 1: 기본 함수 호출

```tsx
function test(callback) {
  callback();
}

test(function () {
  console.log(this);
});
// window (브라우저 기준)
```

---

### 🔹 예시 2: addEventListener

```tsx
document.body.addEventListener('click', function () {
  console.log(this);
});
```

여기서 `this`는

이벤트가 발생한 DOM 요소

👉 `addEventListener`가 `this`를 지정해줌

---

## 5️⃣ 콜백 내부 this를 원하는 값으로 바꾸기

### ✔ bind 사용

```tsx
var obj = {
  name: '철수'
};

setTimeout(function () {
  console.log(this.name);
}.bind(obj), 1000);
```

✔ `bind`로 명시적 바인딩

---

### ✔ 화살표 함수 사용

```tsx
var obj = {
  name: '철수',
  run: function () {
    setTimeout(() => {
      console.log(this.name);
    }, 1000);
  }
};

obj.run();
```

✔ 화살표 함수는 상위 `this` 유지

---

## 6️⃣ 메서드를 콜백으로 넘기면?

```tsx
var obj = {
  name: '영희',
  say: function () {
    console.log(this.name);
  }
};

setTimeout(obj.say, 1000);
```

결과:

```
undefined
```

왜?

- `obj.say`를 넘기는 순간
- 메서드가 아니라 **함수로 실행되기 때문**

✔ 해결:

```tsx
setTimeout(obj.say.bind(obj), 1000);
```

---

# 🔥 콜백 지옥 (Callback Hell)

비동기 작업이 중첩될수록

코드가 깊게 파고들며 가독성이 떨어지는 현상

---

## 1️⃣ 콜백 지옥 예시

```tsx
setTimeout(function () {
  console.log('1단계');

  setTimeout(function () {
    console.log('2단계');

    setTimeout(function () {
      console.log('3단계');
    }, 1000);

  }, 1000);

}, 1000);
```

들여쓰기가 계속 깊어진다.

---

## 2️⃣ 해결 방법 1 — 기명 함수로 분리

```tsx
function step3() {
  console.log('3단계');
}

function step2() {
  console.log('2단계');
  setTimeout(step3, 1000);
}

function step1() {
  console.log('1단계');
  setTimeout(step2, 1000);
}

setTimeout(step1, 1000);
```

✔ 가독성 개선

✔ 구조는 여전히 콜백

---

## 3️⃣ 해결 방법 2 — Promise

```tsx
function delay(msg) {
  return new Promise(function (resolve) {
    setTimeout(function () {
      console.log(msg);
      resolve();
    }, 1000);
  });
}

delay('1단계')
  .then(function () {
    return delay('2단계');
  })
  .then(function () {
    return delay('3단계');
  });
```

✔ 체이닝 가능

✔ 구조가 평평해짐

---

## 4️⃣ 해결 방법 3 — Generator

```tsx
function delay(msg) {
  return new Promise(function (resolve) {
    setTimeout(function () {
      console.log(msg);
      resolve();
    }, 1000);
  });
}

function* gen() {
  yield delay('1단계');
  yield delay('2단계');
  yield delay('3단계');
}
```

Generator는 실행 제어를 외부로 넘길 수 있음

(실무에서는 Promise 기반 라이브러리와 함께 사용)

---

## 5️⃣ 해결 방법 4 — async / await

가장 직관적이고 현대적인 방식

```tsx
function delay(msg) {
  return new Promise(function (resolve) {
    setTimeout(function () {
      console.log(msg);
      resolve();
    }, 1000);
  });
}

async function run() {
  await delay('1단계');
  await delay('2단계');
  await delay('3단계');
}

run();
```

✔ 동기 코드처럼 읽힘

✔ 에러 처리도 `try/catch` 가능

---

# 📌 최종 정리

### 🔹 콜백이란

- 다른 코드에 **인자로 넘기는 함수**
- 실행 제어권을 위임하는 함수

---

### 🔹 제어권을 가진 코드가 결정하는 것

- 콜백 실행 시점
- 전달할 인자 및 순서
- `this` 바인딩 여부

---

### 🔹 중요한 특징

- 메서드를 넘겨도 결국 함수로 실행됨
- 기본 `this`는 전역 객체
- `bind`로 강제 바인딩 가능
- 화살표 함수는 상위 `this` 유지

---

### 🔹 비동기 제어 발전 과정

1. 콜백
2. 기명 함수 분리
3. Promise
4. Generator
5. async / await